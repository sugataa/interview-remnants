# borrowed from functional programming
# map operation -> takes a function f and apply it on a per-element basis to a given list
# reduce operation -> applies a function g to a given list L, but instead of isolating on each element, g accumulates the contents of L into a single result
